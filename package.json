{
  "name": "popup-sentinel",
  "version": "0.0.6",
  "private": true,
  "description": "A policy-driven userscript that blocks verified popup generators.",
  "scripts": {
    "test": "node --test && node scripts/validate.mjs",
    "test:live-mobile": "node scripts/live-mobile-webkit.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
